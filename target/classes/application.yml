spring:
  application:
    name: 'customermanagement'
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
  servlet:
    context-path: /customerdomain/v1/customerManagement